-- Assuming that your object is present in InputRoot.JSON.Object
SET ObjectRef = InputRoot.JSON.Object;

-- Iterate over the keys of the object
WHILE LASTMOVE(ObjectRef) DO
    SET KeyName = NAMEOF(ObjectRef);
    
    -- Your logic for each key goes here
    -- For example, print the key
    -- You can access the corresponding value using ObjectRef.{KeyName}

    -- Print the key
    SET OutputRoot.Properties.Message = 'Key: ' || KeyName;

    -- Move to the next key
    MOVE ObjectRef NEXTSIBLING;
END WHILE;
